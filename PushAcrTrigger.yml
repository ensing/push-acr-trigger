resources:
  containers:
  - container: nginx
    type: ACR
    azureSubscription: MY-Azure-connection
    resourceGroup: test-trigger
    registry: acrtriggertest1
    repository: test
    trigger:
     enabled: true
